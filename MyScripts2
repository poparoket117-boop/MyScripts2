-- DrunixHub (Два окна: функции + игроки)
local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hum = char:FindFirstChild("Humanoid")
local root = char:FindFirstChild("HumanoidRootPart")

local noclip = false
local infJump = false
local god = false
local walkSpeed = 16
local targetPlayer = nil

local funMode = false
local funModeType = "Circle"
local funSpeed = 20
local circleAngle = 0

-- Функции
local function SetGod(state)
    god = state
    if state then
        hum.MaxHealth = 9e9
        hum.Health = 9e9
        hum.BreakJointsOnDeath = false
    else
        hum.MaxHealth = 100
        hum.Health = 100
        hum.BreakJointsOnDeath = true
    end
end

local function SetInfJump(state)
    infJump = state
    if state then
        hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
        hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true)
    end
end

local jumpCon
local function startInfJump()
    if jumpCon then jumpCon:Disconnect() end
    jumpCon = hum.StateChanged:Connect(function(old, new)
        if infJump and (new == Enum.HumanoidStateType.Landed or new == Enum.HumanoidStateType.Freefall) then
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)
end

local function SetNoClip(state)
    noclip = state
    if not char then return end
    for _, v in pairs(char:GetDescendants()) do
        if v:IsA("BasePart") then
            v.CanCollide = not state
        end
    end
end

player.CharacterAdded:Connect(function(newChar)
    char = newChar
    hum = char:WaitForChild("Humanoid")
    root = char:WaitForChild("HumanoidRootPart")
    if noclip then SetNoClip(true) end
    if infJump then SetInfJump(true) startInfJump() end
    if god then SetGod(true) end
    hum.WalkSpeed = walkSpeed
end)

-- Основные циклы
game:GetService("RunService").Heartbeat:Connect(function()
    if noclip and char then
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") and v.CanCollide ~= false then
                v.CanCollide = false
            end
        end
    end
end)

game:GetService("RunService").Heartbeat:Connect(function()
    if god and hum then
        hum.Health = 9e9
        hum.MaxHealth = 9e9
        hum.BreakJointsOnDeath = false
    end
end)

-- Фан-режим
game:GetService("RunService").Heartbeat:Connect(function()
    if not funMode or not root then return end
    
    if funModeType == "Spin" then
        root.CFrame = root.CFrame * CFrame.Angles(0, 0.1 * (funSpeed / 10), 0)
        return
    end
    
    local target = nil
    if targetPlayer then
        local plr = game:GetService("Players"):FindFirstChild(targetPlayer)
        if plr and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            target = plr.Character.HumanoidRootPart
        else
            targetPlayer = nil
        end
    end
    
    if not target then
        local closest = nil
        local closestDist = 50
        for _, p in pairs(game:GetService("Players"):GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local dist = (root.Position - p.Character.HumanoidRootPart.Position).Magnitude
                if dist < closestDist then
                    closestDist = dist
                    closest = p.Character.HumanoidRootPart
                end
            end
        end
        target = closest
    end
    
    if not target then return end
    
    if funModeType == "Circle" then
        local speedFactor = funSpeed / 10
        circleAngle = circleAngle + 0.05 * speedFactor
        local radius = 2
        local offset = Vector3.new(math.cos(circleAngle) * radius, 0, math.sin(circleAngle) * radius)
        local targetPos = target.Position + offset
        root.CFrame = CFrame.new(targetPos, target.Position)
    elseif funModeType == "Follow" then
        local speedFactor = funSpeed / 10
        local direction = (target.Position - root.Position).Unit
        root.CFrame = root.CFrame + direction * (0.5 * speedFactor)
    end
end)

-- GUI (два окна)
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "DrunixHub"
screenGui.ResetOnSpawn = false

-- КНОПКА ВХОДА "DH"
local openBtn = Instance.new("TextButton")
openBtn.Size = UDim2.new(0, 70, 0, 70)
openBtn.Position = UDim2.new(0.5, -35, 0.5, -35)
openBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
openBtn.BackgroundTransparency = 0
openBtn.BorderSizePixel = 2
openBtn.BorderColor3 = Color3.fromRGB(0, 0, 0)
openBtn.Text = "DH"
openBtn.TextColor3 = Color3.fromRGB(180, 0, 0)
openBtn.TextSize = 30
openBtn.Font = Enum.Font.GothamBold
openBtn.Draggable = true
openBtn.Active = true
local openCorner = Instance.new("UICorner", openBtn)
openCorner.CornerRadius = UDim.new(1, 0)

-- ОКНО ФУНКЦИЙ
local funcFrame = Instance.new("Frame")
funcFrame.Size = UDim2.new(0, 250, 0, 380)
funcFrame.Position = UDim2.new(0.2, 0, 0.5, -190)
funcFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
funcFrame.BackgroundTransparency = 0.1
funcFrame.BorderSizePixel = 2
funcFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
funcFrame.Visible = false
funcFrame.Active = true
local funcCorner = Instance.new("UICorner", funcFrame)
funcCorner.CornerRadius = UDim.new(0, 15)

-- Заголовок окна функций
local funcDrag = Instance.new("Frame", funcFrame)
funcDrag.Size = UDim2.new(1, 0, 0, 30)
funcDrag.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
funcDrag.BackgroundTransparency = 0.3
funcDrag.BorderSizePixel = 0
funcDrag.Active = true
funcDrag.Draggable = true
local funcDragCorner = Instance.new("UICorner", funcDrag)
funcDragCorner.CornerRadius = UDim.new(0, 15)

local funcTitle = Instance.new("TextLabel", funcDrag)
funcTitle.Size = UDim2.new(1, 0, 0, 30)
funcTitle.Text = "⚙️ Functions"
funcTitle.TextColor3 = Color3.fromRGB(0, 0, 0)
funcTitle.TextSize = 18
funcTitle.BackgroundTransparency = 1
funcTitle.Font = Enum.Font.GothamBold

local funcClose = Instance.new("TextButton", funcDrag)
funcClose.Size = UDim2.new(0, 30, 0, 30)
funcClose.Position = UDim2.new(1, -35, 0, 0)
funcClose.Text = "✕"
funcClose.TextColor3 = Color3.fromRGB(0, 0, 0)
funcClose.TextSize = 18
funcClose.BackgroundTransparency = 1
funcClose.Font = Enum.Font.GothamBold
funcClose.MouseButton1Click:Connect(function()
    funcFrame.Visible = false
    playerFrame.Visible = false
end)

-- Кнопки в окне функций
local function MakeFuncButton(parent, text, yPos, callback)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(0.85, 0, 0, 28)
    btn.Position = UDim2.new(0.075, 0, yPos, 0)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(0, 0, 0)
    btn.TextSize = 13
    btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    btn.BackgroundTransparency = 0.15
    btn.BorderSizePixel = 1
    btn.BorderColor3 = Color3.fromRGB(0, 0, 0)
    local c = Instance.new("UICorner", btn)
    c.CornerRadius = UDim.new(0, 8)
    btn.MouseButton1Click:Connect(function()
        callback(btn)
    end)
    return btn
end

local function MakeFuncSlider(parent, labelText, yPos, minVal, maxVal, defaultVal, callback)
    local label = Instance.new("TextLabel", parent)
    label.Size = UDim2.new(0.85, 0, 0, 16)
    label.Position = UDim2.new(0.075, 0, yPos, 0)
    label.Text = labelText .. ": " .. defaultVal
    label.TextColor3 = Color3.fromRGB(0, 0, 0)
    label.TextSize = 11
    label.BackgroundTransparency = 1
    label.Font = Enum.Font.Gotham
    
    local slider = Instance.new("Frame", parent)
    slider.Size = UDim2.new(0.85, 0, 0, 5)
    slider.Position = UDim2.new(0.075, 0, yPos + 0.035, 0)
    slider.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    slider.BackgroundTransparency = 0.3
    slider.BorderSizePixel = 1
    slider.BorderColor3 = Color3.fromRGB(0, 0, 0)
    local sCorner = Instance.new("UICorner", slider)
    sCorner.CornerRadius = UDim.new(1, 0)
    
    local fill = Instance.new("Frame", slider)
    fill.Size = UDim2.new(0.2, 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
    fill.BorderSizePixel = 0
    
    local btn = Instance.new("TextButton", slider)
    btn.Size = UDim2.new(0, 14, 0, 14)
    btn.Position = UDim2.new(0.2, -7, 0.5, -7)
    btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    btn.BorderSizePixel = 1
    btn.BorderColor3 = Color3.fromRGB(0, 0, 0)
    btn.Text = ""
    local bCorner = Instance.new("UICorner", btn)
    bCorner.CornerRadius = UDim.new(1, 0)
    
    local dragging = false
    btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
        end
    end)
    btn.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
    game:GetService("UserInputService").InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) then
            local x = input.Position.X
            local absX = slider.AbsolutePosition.X
            local sizeX = slider.AbsoluteSize.X
            if sizeX > 0 then
                local pos = math.clamp((x - absX) / sizeX, 0, 1)
                fill.Size = UDim2.new(pos, 0, 1, 0)
                btn.Position = UDim2.new(pos, -7, 0.5, -7)
                local val = math.round(minVal + pos * (maxVal - minVal))
                label.Text = labelText .. ": " .. val
                callback(val)
            end
        end
    end)
end

-- Создание кнопок в окне функций
local fy = 0.08

local ncBtn = MakeFuncButton(funcFrame, "NoClip: OFF", fy, function(btn)
    noclip = not noclip
    SetNoClip(noclip)
    btn.Text = noclip and "NoClip: ON" or "NoClip: OFF"
    btn.TextColor3 = noclip and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(0, 0, 0)
end)
fy = fy + 0.07

local ijBtn = MakeFuncButton(funcFrame, "InfJump: OFF", fy, function(btn)
    infJump = not infJump
    SetInfJump(infJump)
    if infJump then startInfJump() end
    btn.Text = infJump and "InfJump: ON" or "InfJump: OFF"
    btn.TextColor3 = infJump and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(0, 0, 0)
end)
fy = fy + 0.07

local godBtn = MakeFuncButton(funcFrame, "God Mode: OFF", fy, function(btn)
    god = not god
    SetGod(god)
    btn.Text = god and "God Mode: ON" or "God Mode: OFF"
    btn.TextColor3 = god and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(0, 0, 0)
end)
fy = fy + 0.07

MakeFuncSlider(funcFrame, "Walk Speed", fy, 1, 500, 16, function(v)
    walkSpeed = v
    if hum then hum.WalkSpeed = v end
end)
fy = fy + 0.085

local funBtn = MakeFuncButton(funcFrame, "Fun Mode: OFF", fy, function(btn)
    funMode = not funMode
    btn.Text = funMode and "Fun Mode: ON" or "Fun Mode: OFF"
    btn.TextColor3 = funMode and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(0, 0, 0)
end)
fy = fy + 0.07

local modeBtn = MakeFuncButton(funcFrame, "Mode: Circle", fy, function(btn)
    local modes = {"Circle", "Follow", "Spin"}
    local current = 1
    for i, v in ipairs(modes) do
        if v == funModeType then current = i break end
    end
    current = current % 3 + 1
    funModeType = modes[current]
    btn.Text = "Mode: " .. funModeType
end)
fy = fy + 0.07

MakeFuncSlider(funcFrame, "Fun Speed", fy, 1, 100, 20, function(v)
    funSpeed = v
end)
fy = fy + 0.085

local allBtn = MakeFuncButton(funcFrame, "Enable All", fy, function(btn)
    noclip = true
    infJump = true
    god = true
    funMode = true
    SetNoClip(true)
    SetInfJump(true)
    startInfJump()
    SetGod(true)
    ncBtn.Text = "NoClip: ON"
    ncBtn.TextColor3 = Color3.fromRGB(0, 255, 0)
    ijBtn.Text = "InfJump: ON"
    ijBtn.TextColor3 = Color3.fromRGB(0, 255, 0)
    godBtn.Text = "God Mode: ON"
    godBtn.TextColor3 = Color3.fromRGB(0, 255, 0)
    funBtn.Text = "Fun Mode: ON"
    funBtn.TextColor3 = Color3.fromRGB(0, 255, 0)
end)

-- ОКНО ИГРОКОВ
local playerFrame = Instance.new("Frame")
playerFrame.Size = UDim2.new(0, 200, 0, 300)
playerFrame.Position = UDim2.new(0.6, 0, 0.5, -150)
playerFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
playerFrame.BackgroundTransparency = 0.1
playerFrame.BorderSizePixel = 2
playerFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
playerFrame.Visible = false
playerFrame.Active = true
local playerCorner = Instance.new("UICorner", playerFrame)
playerCorner.CornerRadius = UDim.new(0, 15)

-- Заголовок окна игроков
local playerDrag = Instance.new("Frame", playerFrame)
playerDrag.Size = UDim2.new(1, 0, 0, 30)
playerDrag.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
playerDrag.BackgroundTransparency = 0.3
playerDrag.BorderSizePixel = 0
playerDrag.Active = true
playerDrag.Draggable = true
local playerDragCorner = Instance.new("UICorner", playerDrag)
playerDragCorner.CornerRadius = UDim.new(0, 15)

local playerTitle = Instance.new("TextLabel", playerDrag)
playerTitle.Size = UDim2.new(1, 0, 0, 30)
playerTitle.Text = "👥 Players"
playerTitle.TextColor3 = Color3.fromRGB(0, 0, 0)
playerTitle.TextSize = 18
playerTitle.BackgroundTransparency = 1
playerTitle.Font = Enum.Font.GothamBold

local playerClose = Instance.new("TextButton", playerDrag)
playerClose.Size = UDim2.new(0, 30, 0, 30)
playerClose.Position = UDim2.new(1, -35, 0, 0)
playerClose.Text = "✕"
playerClose.TextColor3 = Color3.fromRGB(0, 0, 0)
playerClose.TextSize = 18
playerClose.BackgroundTransparency = 1
playerClose.Font = Enum.Font.GothamBold
playerClose.MouseButton1Click:Connect(function()
    playerFrame.Visible = false
    funcFrame.Visible = false
end)

-- Список игроков
local playerScroll = Instance.new("ScrollingFrame", playerFrame)
playerScroll.Size = UDim2.new(1, 0, 1, -30)
playerScroll.Position = UDim2.new(0, 0, 0, 30)
playerScroll.BackgroundTransparency = 1
playerScroll.BorderSizePixel = 0
playerScroll.ScrollBarThickness = 4
playerScroll.ScrollBarImageColor3 = Color3.fromRGB(0, 0, 0)
playerScroll.ScrollBarImageTransparency = 0.5

local playerLayout = Instance.new("UIListLayout", playerScroll)
playerLayout.FillDirection = Enum.FillDirection.Vertical
playerLayout.Padding = UDim.new(0, 4)
playerLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local function RefreshPlayerList()
    for _, child in pairs(playerScroll:GetChildren()) do
        if child:IsA("TextButton") then
            child:Destroy()
        end
    end
    local list = {}
    for _, p in pairs(game:GetService("Players"):GetPlayers()) do
        if p ~= player then
            table.insert(list, p.Name)
        end
    end
    if #list == 0 then
        table.insert(list, "No players")
    end
    playerScroll.CanvasSize = UDim2.new(0, 0, 0, #list * 32 + 10)
    for _, item in ipairs(list) do
        local btn = Instance.new("TextButton", playerScroll)
        btn.Size = UDim2.new(0.9, 0, 0, 28)
        btn.Text = item
        btn.TextColor3 = Color3.fromRGB(0, 0, 0)
        btn.TextSize = 13
        btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        btn.BackgroundTransparency = 0.1
        btn.BorderSizePixel = 1
        btn.BorderColor3 = Color3.fromRGB(0, 0, 0)
        local bCorner = Instance.new("UICorner", btn)
        bCorner.CornerRadius = UDim.new(0, 8)
        btn.MouseButton1Click:Connect(function()
            if item == "No players" then return end
            targetPlayer = item
            -- Подсвечиваем выбранного
            for _, child in pairs(playerScroll:GetChildren()) do
                if child:IsA("TextButton") then
                    child.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                    child.BackgroundTransparency = 0.1
                end
            end
            btn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
            btn.BackgroundTransparency = 0.3
            print("Target set to: " .. item)
        end)
    end
end

RefreshPlayerList()

-- Открытие меню (оба окна)
openBtn.MouseButton1Click:Connect(function()
    local vis = not funcFrame.Visible
    funcFrame.Visible = vis
    playerFrame.Visible = vis
    if vis then
        RefreshPlayerList()
    end
end)

-- Родители
openBtn.Parent = screenGui
funcFrame.Parent = screenGui
playerFrame.Parent = screenGui

-- Безопасное добавление GUI
local success, err = pcall(function()
    screenGui.Parent = game:GetService("CoreGui")
end)
if not success then
    screenGui.Parent = player:WaitForChild("PlayerGui")
end

print("DrunixHub загружен! Нажми на кнопку DH.")
